<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.felixfavour.mobotithe">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_mobotithe_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar.SplashActivity">
        <activity android:noHistory="true" android:name=".gui.view.onboarding.OnboardingWelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".gui.view.SplashActivity" android:noHistory="true" />
        <activity
            android:name=".gui.view.settings.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".gui.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.felixfavour.mobotithe.gui.MainActivity" />
        </activity>
        <activity android:noHistory="true" android:name=".gui.view.login.LoginActivity" />
        <activity
            android:name=".gui.MainActivity"
            android:label="@string/title_activity_main"></activity>
    </application>

</manifest>